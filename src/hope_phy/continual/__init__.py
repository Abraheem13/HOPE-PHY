"""[Day 8] Continual-learning baselines: EWC, replay buffer, naive fine-tune.
Each exposes .observe(x, y) mirroring ttt.engine.TTTEngine so streaming eval
treats all adaptation methods identically."""
