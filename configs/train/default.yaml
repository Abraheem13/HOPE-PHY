epochs: 300
batch_size: 256
lr: 1.0e-3
lr_step: 100        # LR drops at epoch 100 and 200 (now actually triggers)
lr_gamma: 0.3
grad_clip: 1.0
cms_lr_slow: 1.0e-4
cms_lr_ratio: 6.0
anchor_decay: 0.995
workers: 4
